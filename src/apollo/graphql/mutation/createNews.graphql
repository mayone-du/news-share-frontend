mutation CreateNews($url: String!, $selectCategoryId: ID, $tagIds: [ID]) {
  createNews(input: { url: $url, selectCategoryId: $selectCategoryId, tagIds: $tagIds }) {
    news {
      id
      title
      url
      createdAt
    }
  }
}
